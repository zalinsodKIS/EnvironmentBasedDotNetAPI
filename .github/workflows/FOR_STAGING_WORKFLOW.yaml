name: Build and deploy ASP.Net Core app to an Azure Web App

on:
  workflow_dispatch:
  push:
    branches: [ "FOR_STAGING" ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Display Environment Variables
        run: echo ${{ env.ACCESS_KEYS }}
